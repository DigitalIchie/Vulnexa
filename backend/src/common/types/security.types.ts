export enum Role {
  admin = 'admin',
  tester = 'tester',
}

export enum ScanStatus {
  queued = 'queued',
  running = 'running',
  completed = 'completed',
  failed = 'failed',
}

export enum Severity {
  low = 'low',
  medium = 'medium',
  high = 'high',
}
